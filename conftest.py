"""Pytest configuration that puts the repo root on sys.path for test imports."""

# Presence of this file puts the repo root on sys.path so tests can
# `import sklearn_worker`, `import serve`, and `import vgi_sklearn`.
